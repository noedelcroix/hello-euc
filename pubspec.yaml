name: hello_euc
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  collection: ^1.17.2
  easy_folder_picker: ^1.3.1
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3
  flutter_gundb: ^0.0.3
  get_it: ^7.6.4
  graphic: ^2.2.1
  location: ^5.0.3
  maplibre_gl:
      git:
        url: https://github.com/maplibre/flutter-maplibre-gl.git
        ref: main
  permission_handler: ^10.4.5
  settings_ui: ^2.0.2
  shared_preferences: ^2.2.2
  wakelock: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
